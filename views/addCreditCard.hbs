<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link rel="icon" href="/images/favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Add Cart</title>
</head>
<body>
     <header>
        <a href="/products">
                <img src="/images/logo.svg" >
        </a>

        <div class="header-actions">

            {{#if isNotPremium}}
                <a href="/session/logout">
                    <i class="bi bi-box-arrow-left"></i>
                </a>
                {{else}}
                <div class="premium-button">Premium</div>
                <a href="/session/logout" class="logout-button">
                    <i class="bi bi-box-arrow-left"></i>
                </a>
            {{/if}}
        </div>
       

    </header>
    <main class="add-credit">
        <h1>Añadir metodo de pago</h1>
        <form action="/api/cart/{{user.cart}}/purchase" method="POST" class="credit-card-form">
            
            <div class="w-100">
                <label for="nombre" class="label">Nombre del titular:</label>
                <input type="text" class="input" required id="nombre" name="nombre">
            </div>
            <div class="w-100">
                <label for="numero"  class="label">Número de Tarjeta:</label>
                <input type="text" id="numero" class="input" name="numero" pattern="[0-9]{16}" required placeholder="16 dígitos">
            </div>

            <div class="w-50">
                <div>
                    <label for="vencimiento"  class="label">Fecha de Vencimiento:</label>
                    <input type="text" id="vencimiento" class="input" name="vencimiento" pattern="(0[1-9]|1[0-2])\/[0-9]{2}" required placeholder="MM/YY">
                </div>
                
                <div>
                    <label for="cvv"  class="label">CVV:</label>
                    <input type="text" id="cvv" name="cvv" class="input" pattern="[0-9]{3}" required placeholder="3 dígitos">
                </div>
                
            </div>

            <input type="submit"  class="add-product-button"/>
        </form>
    </main>
</body>
</html>