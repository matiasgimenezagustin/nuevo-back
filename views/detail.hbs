<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
     <link rel="icon" href="/images/favicon.png" type="image/x-icon">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Detalle</title>
</head>
<body>
    <header>
         <a href="/products">
                <img src="/images/logo.svg" >
        </a>

        <div class="header-actions">
           
          
            {{#if isNotPremium}}
                <a href="/session/logout">
                    <i class="bi bi-box-arrow-left"></i>
                </a>
                {{else}}
                <div class="premium-button">Premium</div>
                <a href="/session/logout" class="logout-button">
                    <i class="bi bi-box-arrow-left"></i>
                </a>
            {{/if}}
        </div>
       

    </header>
   <main class="detail">
     <div class="left">
        <img src="{{product.thumbnail}}" class="product-img"/>
        <div class="content">
            <h1>{{product.title}}</h1>
            <span>${{product.price}}</span>
        </div>
        
    </div>
    <div class="rigth">
        <h2>Descripcion del producto:</h2>
        <p>{{product.description}}</p>
        <h3>Stock disponible: {{product.stock}}</h3>
      
        {{#if isOwner}}
            <div class="actions-container">
                <form action="/products/edit/{{product._id}}" method="post">
                    <button class="edit-btn">Editar <i class="bi bi-pencil-square"></i></button>
                </form>
                
                <form action="/products/delete/{{product._id}}" method="POST">
                    <button type="submit" class="delete-btn">Eliminar <i class="bi bi-trash3"></i></button>
                </form>
                
            </div>
            
            {{else}}
            
            <form action="/api/cart/{{user.cart}}/products/{{product._id}}" method="POST">
                <button type="submit" class="add-product-button">Comprar</button>
            </form>
        {{/if}}
    </div>
   </main>



   
   
   
    
</body>
</html>